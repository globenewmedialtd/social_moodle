langcode: en
status: true
dependencies:
  module:
    - workflows_field
id: application
label: Application
type: workflows_field
type_settings:
  states:
    applied:
      label: Applied
      weight: 0
    approved:
      label: Approved
      weight: 1
    denied:
      label: Denied
      weight: 2
    waitlist:
      label: Waitlist
      weight: 3
  transitions:
    approve:
      label: Approve
      from:
        - applied
      to: approved
      weight: 0
    deny:
      label: Deny
      from:
        - applied
      to: denied
      weight: 1
    waitlist:
      label: Waitlist
      from:
        - applied
      to: waitlist
      weight: 2
  initial_state: applied

